# بنية RAFIQ UMRAH AI Marketplace

## الأدوار والصلاحيات

| الدور | المجال | الضوابط الرئيسية |
| --- | --- | --- |
| `user` | البحث، الحفظ، طلب عمرة، طلب عرض، مراجعاته | لا يرى إلا ملفه وطلباته ومفضلاته. |
| `agency` | ملف الوكالة والعروض والـLeads المطابقة لها | لا يقرأ بيانات Lead إلا بعد مطابقة تمنحها الإدارة أو النظام. |
| `admin` | مراجعة الوكالات والعروض والمراجعات والـLeads | كل تغيير حساس يدخل سجل التدقيق. |
| `super_admin` | الإعدادات والأوزان والباقات والتدقيق | لا يُمنح من الواجهة؛ ترقية محكومة بقاعدة البيانات. |

## نواة البيانات

| الكيان | الغرض | ملاحظات النزاهة |
| --- | --- | --- |
| `users` و`profiles` | هوية المستخدم ودوره وبياناته العامة | معرف OAuth لا يُثق به من العميل؛ رقم الهاتف اختياري ومتحقق منه. |
| `agencies` و`agency_documents` | ملف الوكالة ووثائقها وحالة التحقق | حالات `pending`, `verified`, `rejected`, `suspended`؛ التحقق داخلي للمنصة فقط. |
| `offers` و`offer_media` | عروض العمرة ومواصفاتها وتواريخها وتوفرها | لا يُعرض العرض النشط إذا انتهى أو لم يعتمد؛ السعر يحمل تاريخ تحديث. |
| `umrah_requests` | طلب المعتمر المخصص | بيانات شخصية محمية ومرتبطة بمالك الطلب. |
| `leads` و`lead_matches` | مسار وصول الطلب المناسب إلى وكالة | تسليم بيانات الاتصال يتم على أساس مطابقة مصرح بها. |
| `favorites` و`comparisons` | اختيارات المستخدم المحفوظة | لا تتطلب ظهور بيانات خاصة. |
| `reviews` | مراجعات قابلة للتتبع والمراجعة | لا تُزرع مراجعات؛ الحالة تبدأ `pending` عند تفعيلها. |
| `offer_events` | مشاهدات، طلبات، نقرات WhatsApp بلا PII | أساس تحليلات حقيقية وقابلة للتجميع. |
| `audit_logs` | إجراءات الإدارة الحساسة | الفاعل، الكيان، الإجراء، الوقت، والبيانات الوصفية المحدودة. |
| `settings` | أوزان التوصية وإعدادات المنصة | تدار من `super_admin` فقط. |

## قواعد الوصول الأساسية

تُطبق الصلاحيات داخل إجراءات الخادم. يمكن لصاحب الوكالة تعديل وكالة يملكها وعروضها فقط، ولا يمكنه تغيير حالة التحقق أو وضع العرض المميز. يتعامل المستخدم مع طلباته ومفضلاته فقط. الوصول إلى أرقام الهاتف ووثائق الوكالة وتفاصيل Lead محكوم بإجراء خادم مخصص وليس بحالة واجهة أو معرف قادم من العميل.

## مسارات المنتج

| المسار | الحالة | المقصود |
| --- | --- | --- |
| `/` | عام | بحث ذكي وشرح المنتج ومصادر الثقة. |
| `/offers` و`/offers/:id` | عام | بحث مرقم وفلاتر ومقارنة وطلب عرض مع مصدر وتاريخ تحديث. |
| `/request-umrah` | عام / حساب اختياري | إنشاء طلب عمرة مع تحقق صارم من الهاتف والميزانية والتواريخ. |
| `/agencies` و`/agencies/:slug` | عام | وكالات مع حالة التحقق الداخلية وعروضها الفعالة فقط. |
| `/for-agencies` | عام | اكتساب الوكالات وباقات جاهزة دون دفع حقيقي. |
| `/account` | مستخدم | ملفه وطلباته ومفضلاته ومقارناته. |
| `/agency/dashboard` | وكالة | عروضها وLeads المصرح بها وتحليلاتها الفعلية وحالاتها الفارغة. |
| `/admin` | إدارة | مراجعة الكيانات وسجل التدقيق ومؤشرات قائمة على بيانات حقيقية فقط. |

## محرك التوصية

يستخرج الخادم أولًا عروضًا نشطة ومطابقة للقيود الحتمية، ويحسب درجات قابلة للضبط للسعر والتاريخ والمدة والمسافة والخدمات والطيران. يمكن لنموذج لغوي لاحقًا توليد **تفسير موجز فقط** من قائمة العرض المصرح بها ونتيجة الحساب. لا يحق له إدخال عرض أو سعر أو فندق أو مقعد أو وكالة غير موجودة في المدخلات المنظمة.

## الحدود الحالية

لا يشمل الإصدار الأساسي مدفوعات أو توثيقًا حكوميًا أو نشر تقييمات غير مراجعة. كما لا يقوم بتحويل بيانات شخصية إلى أدوات التحليل أو رسائل WhatsApp قبل تحقق البيانات وإقرار المستخدم بالإرسال.
